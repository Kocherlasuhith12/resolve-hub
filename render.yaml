# Render Blueprint Specification for ResolveHub
# https://render.com/docs/blueprint-spec

services:
  # 1. FastAPI Backend API Service
  - type: web
    name: resolvehub-api-suhith
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    region: oregon
    healthCheckPath: /health/live
    envVars:
      - key: RH_ENVIRONMENT
        value: production
      - key: RH_JWT_SECRET
        generateValue: true
      - key: RH_DATABASE_URL
        fromDatabase:
          name: resolvehub-db-suhith
          property: connectionString
      - key: RH_REDIS_URL
        fromService:
          type: redis
          name: resolvehub-redis-suhith
          property: connectionString
      - key: RH_SERVE_FRONTEND
        value: "false"
      - key: RH_STRIPE_SECRET_KEY
        sync: false
      - key: RH_STRIPE_PUBLISHABLE_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false

  # 2. Redis Cache Instance
  - type: redis
    name: resolvehub-redis-suhith
    plan: free
    ipAllowList: []

  # 3. React Frontend Static Site
  - type: web
    name: resolvehub-frontend-suhith
    runtime: static
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: ./frontend/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_BASE_URL
        fromService:
          type: web
          name: resolvehub-api-suhith
          property: connectionString

databases:
  # 4. PostgreSQL Database Instance
  - name: resolvehub-db-suhith
    plan: free
    databaseName: resolvehub
    user: resolvehub_user
